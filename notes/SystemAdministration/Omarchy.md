
## Monitors
monitor configuration: no need for monmon
identify monitor by its description string regardless of which port is it connected to

## Fonts
There is a TUI for changing the font, but not for changing font size!
Terminal font size is configured in TODO
Top bar (Waybar) font size is configured in TODO

## JetBrains
JetBrains IDEs are available from AUR, but the best way to install and update them is via JetBrains Toolbox, which is also available from AUR :)

TODO Wayland

## BitWarden
I do not use `1Password` password manager that comes pre-installed with Omarchy;
I use a different password manager - `Bitwarden`. It is available from `AUR`.

I changed the keyboard binding from `1Password` to `Bitwarden` in TODO:
```toml
bindd = 
```